source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in active_record_auditable.gemspec.
gemspec

group :development, :test do
  gem "factory_bot_rails"
  gem "pry-rails"
  gem "rspec-rails"
  gem "sqlite3"
end
